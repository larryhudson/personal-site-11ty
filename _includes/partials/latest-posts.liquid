<h2>Latest posts</h2>

<ul>
  {%- assign posts = collections.post | reverse -%}
  {% for post in posts %}
  <li>
    <a href="{{ post.url }}"
      >{{ post.data.title }} ({{ post.date | dateFilter }})</a
    >
  </li>
  {% endfor %}
</ul>
