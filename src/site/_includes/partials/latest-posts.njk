<h2>Latest posts</h2>

<ul>
  {% for post in collections.post | reverse %}
  <li>
    <a href="{{ post.url }}"
      >{{ post.data.title }} ({{ post.date | readableDate }})</a
    >
  </li>
  {% endfor %}
</ul>